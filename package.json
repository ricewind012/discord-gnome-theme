{
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && mkdir -p dist && npx postcss src/index.scss --dir dist --ext css"
	},
	"devDependencies": {
		"prettier": "^3.4.2"
	},
	"dependencies": {
		"@csstools/postcss-sass": "^5.1.1",
		"postcss-cli": "^11.0.0",
		"postcss-functions": "^4.0.2",
		"postcss-scss": "^4.0.9"
	}
}
