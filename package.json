{
	"type": "module",
	"scripts": {
		"build": "rm -rf dist && mkdir -p dist && npx postcss --verbose src --base src --dir dist --ext css"
	},
	"dependencies": {
		"@csstools/postcss-sass": "^5.1.1",
		"postcss-cli": "^11.0.0",
		"postcss-scss": "^4.0.9"
	}
}
